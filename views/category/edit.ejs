<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Category - Woofy Admin</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
  <link rel="stylesheet" href="/css/woofy-theme.css" />
  <link rel="stylesheet" href="/css/admin.css" />
</head>
<body class="admin-body">
  <%- include("../partials/admin-sidebar", { currentPage: 'categories' }) %>

  <div class="admin-sidebar-overlay" id="sidebarOverlay" onclick="toggleSidebar()"></div>

  <header class="admin-topbar">
    <div class="admin-topbar-left">
      <button class="admin-toggle-sidebar" onclick="toggleSidebar()" aria-label="Toggle sidebar">
        <i class="fas fa-bars"></i>
      </button>
      <div class="admin-topbar-search">
        <i class="fas fa-search"></i>
        <input type="text" placeholder="Search categories..." />
      </div>
    </div>
    <div class="admin-topbar-right">
      <div class="admin-profile">
        <div class="avatar"><%= (currentUser && currentUser.username) ? currentUser.username.charAt(0).toUpperCase() : 'A' %></div>
        <span class="name"><%= (currentUser && currentUser.username) ? currentUser.username : 'Admin' %></span>
      </div>
    </div>
  </header>

  <main class="admin-main">
    <%- include("../partials/admin-breadcrumb", { pageTitle: 'Edit Category', pageIcon: 'fas fa-edit' }) %>
    <div class="admin-card admin-form-card">
      <div class="admin-card-body">
        <span class="form-badge">Woofy • Category</span>
        <h1 class="form-title">Edit Category</h1>
        <p class="form-subtitle">Update the category name and description.</p>

        <form action="/categories/update/<%= category._id %>" method="POST">
          <div class="admin-form-group">
            <label class="admin-form-label">Category Name</label>
            <input type="text" name="name" class="admin-form-input" value="<%= category.name %>" required>
          </div>

          <div class="admin-form-group">
            <label class="admin-form-label">Description</label>
            <textarea name="description" class="admin-form-textarea" placeholder="Describe this category..."><%= category.description || '' %></textarea>
          </div>

          <div class="admin-form-actions">
            <button type="submit" class="admin-btn-primary"><i class="fas fa-save"></i> Update Category</button>
            <a href="/categories" class="admin-btn-secondary"><i class="fas fa-arrow-left"></i> Back to Categories</a>
          </div>
        </form>
      </div>
    </div>
  </main>

  <script>
    function toggleSidebar() {
      document.getElementById('adminSidebar').classList.toggle('open');
      document.getElementById('sidebarOverlay').classList.toggle('show');
    }
  </script>
</body>
</html>
